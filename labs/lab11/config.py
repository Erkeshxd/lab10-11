def config():
    return {
        'host': 'localhost',       # Database host (can be 'localhost' or an IP address)
        'port': '5432',            # Database port (default for PostgreSQL is 5432)
        'dbname': 'your_database', # Name of your database
        'user': 'your_user',       # Database username
        'password': 'your_password' # Database password
    }
